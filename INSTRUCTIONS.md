# CURRENT DEVELOPMENT & MODIFICATION INSTRUCTION
# 버전: 2026-08-05 확정
# 상태: 활성 (Active)
# 아카이브: .agents/AGENTS_ARCHIVE_HFT_INFRA_V25.2.md (초기 HFT 인프라 Instruction 보관)

---

## 1. 현재 프로젝트 단계

현재 프로젝트는 최종 리팩토링 이전의 전략 및 기능 개선·검증 단계이다.

현재 단계의 최우선 목적은 다음과 같다.

«전략 1~9 및 Risk, Sensor, 진입/청산, Hedge 및 기타 기능을 최대한 완성하고 검증한다.»

현재 단계에서는 최종 Virtual Broker 리팩토링을 실행하지 않는다.

---

## 2. 향후 최종 리팩토링 방향

향후 최종 리팩토링에서는 다음과 같은 구조를 목표로 한다.

Strategy 1~9
→ Strategy Contract
→ Order / Signal Interface
→ Virtual Broker
→ Account / Order / Execution / Slippage / Margin / Position / Settlement / PnL
→ 향후 Real Broker Adapter

따라서 현재 단계에서 전략, Risk, Sensor, 진입/청산, Hedge 및 기타 기능을 수정·추가할 때에는 위의 향후 구조와 불필요하게 충돌하는 구조를 새롭게 만들지 않는다.

단, 현재 단계에서는 위 구조를 현재 코드에 강제로 선반영하지 않는다.

---

## 3. 현재 테스트 환경 보존 원칙

현재 가상 테스트 환경과 기존 실행 구조를 최대한 유지한다.

반드시 지켜야 할 사항

1. 현재 가상 테스트가 정상적으로 실행되는 상태를 유지한다.
2. 현재 테스트에 필요한 기존 인터페이스, 데이터 흐름 및 실행 구조를 AI가 임의로 변경하지 않는다.
3. 현재 테스트를 유지하기 위해 필요한 기존 구현은 유지한다.
4. 단순히 미래 Virtual Broker 구조와 맞추기 위해 현재 정상적으로 동작하는 코드를 불필요하게 리팩토링하지 않는다.
5. 현재 단계에서 대규모 아키텍처 변경, 계층 재배치 또는 전체 구조 전환을 수행하지 않는다.
6. 현재 테스트 환경을 유지하는 것을 중요하게 취급하되, 전략 및 기능의 실질적인 개선에 필요한 코드 수정 자체를 금지하는 것은 아니다.

즉,

«기능 개선은 현재 단계에서 수행할 수 있다.»

그러나

«최종 아키텍처 전환은 리팩토링 단계에서 수행한다.»

---

## 4. 미래 구조를 고려한 신규 코드 작성 원칙

현재 구조를 유지하면서도 향후 다음 구조로 분리하기 쉬운 형태로 신규 코드를 작성한다.

Strategy
→ Strategy Contract
→ Order / Signal
→ Broker

전략 내부 로직과 Broker의 역할을 새롭게 뒤섞지 않는다.

전략 수정 시 다음 기능을 전략 코드 안에 새롭게 직접 구현하지 않는다.

- 계좌 관리
- 주문 체결
- 증거금 관리
- 슬리피지 처리
- 포지션 관리
- 정산
- Ledger 관리

현재 코드에 이미 존재하는 이러한 기능을 현재 테스트 때문에 유지해야 하는 경우에는 임의로 제거하거나 이동하지 않는다.

다만 신규 기능을 작성할 때는 향후 해당 기능을 별도의 계층이나 모듈로 분리하기 어렵게 만드는 구조를 새롭게 만들지 않는다.

---

## 5. 전략 및 파라미터 보존 원칙

기존 전략 1~9의 조건, 파라미터 및 테스트 결과를 AI가 임의로 변경하지 않는다.

변경이 필요한 경우 반드시 다음을 명시한다.

- 변경 이유
- 변경 대상
- 변경 전
- 변경 후
- 변경으로 인한 영향
- 기존 테스트에 미치는 영향

전략의 성능이나 구조를 개선한다는 이유만으로 기존 조건이나 숫자를 AI가 임의로 변경하지 않는다.

사용자가 명시적으로 변경을 요청한 경우에만 해당 변경을 수행한다.

---

## 6. 신규 기능 추가 원칙

전략, Risk, Sensor, 진입, 청산, Hedge 및 기타 기능을 추가할 수 있다.

단, 신규 기능을 추가할 때 다음을 확인한다.

1. 현재 테스트 환경에 미치는 영향
2. 기존 전략과의 호환성
3. 기존 인터페이스와의 호환성
4. 향후 Strategy Contract와의 충돌 가능성
5. 향후 Broker Interface와의 충돌 가능성
6. 향후 Virtual Broker 구조로 분리할 때 필요한 작업

새로운 기능이 향후 Strategy Contract 또는 Broker Interface와 충돌할 가능성이 있는 경우 AI가 임의로 해결하지 않는다.

---

## 7. 미래 리팩토링 충돌 처리 원칙

현재 구조와 향후 리팩토링 구조 사이에 충돌 가능성이 발견되면 AI가 임의로 구조를 변경하지 않는다.

다음 형식으로 보고한다.

[FUTURE_REFACTORING_CONFLICT]

현재 구조:
변경하려는 부분:
향후 구조와의 충돌 가능성:
권장 해결 방법:
현재 테스트에 미치는 영향:
리팩토링 단계에서 필요한 작업:

충돌 사항을 발견했다고 해서 현재 작업을 임의로 중단하거나 전체 구조를 변경하지 않는다.

사용자의 판단이 필요한 경우 해당 충돌을 명확하게 보고하고 판단을 요청한다.

---

## 8. 미래 구조의 선제적 구현 금지

다음 원칙을 반드시 구분한다.

«미래 구조를 고려한다.»

와

«미래 구조를 현재 코드에 선제적으로 구현한다.»

는 서로 다른 의미이다.

현재 단계에서는 첫 번째만 수행한다.

즉:

- 미래 Virtual Broker 구조를 고려한다.
- 미래 Strategy Contract를 고려한다.
- 미래 Real Broker Adapter를 고려한다.
- 향후 분리될 기능이 무엇인지 고려한다.

그러나 현재 단계에서:

- Virtual Broker를 미리 구현하지 않는다.
- 현재 전략을 새로운 Broker 구조로 강제 이전하지 않는다.
- 전체 아키텍처를 미리 재구성하지 않는다.
- 현재 정상 작동하는 코드를 미래 구조에 맞추기 위해 불필요하게 이동하지 않는다.

---

## 9. AI의 임의 설계 변경 금지

AI는 현재 작업의 범위를 넘어 시스템 구조를 임의로 변경하지 않는다.

특히 다음과 같은 판단을 AI가 독자적으로 수행하지 않는다.

- 전략 구조 전체 변경
- Strategy Contract 임의 변경
- Broker 구조 임의 변경
- 데이터 흐름 전체 변경
- 기존 테스트 방식 변경
- 기존 파라미터 변경
- 기존 전략의 진입/청산 조건 변경
- 기존 Risk 조건 변경

필요하다고 판단되는 경우 먼저 다음 형식으로 제안한다.

[DESIGN_CHANGE_PROPOSAL]

변경이 필요한 이유:
현재 구조:
제안하는 변경:
예상되는 장점:
예상되는 위험:
현재 테스트 영향:
향후 리팩토링 영향:
사용자 승인 필요 여부:

사용자의 승인 없이 범위를 넘어서는 구조 변경을 수행하지 않는다.

---

## 10. 현재 단계에서 허용되는 작업

다음 작업은 현재 단계에서 수행할 수 있다.

- 전략 1~9 분석 및 개선
- 전략 진입 조건 개선
- 전략 청산 조건 개선
- Hedge 로직 개선
- Risk 로직 개선
- Sensor 추가 및 개선
- 지표 및 데이터 처리 개선
- 테스트 로직 개선
- 버그 수정
- 성능 개선
- 예외 처리 개선
- 로그 및 검증 기능 개선
- 현재 가상 테스트를 위한 필요한 기능 추가

단, 이러한 작업은 현재 테스트 환경을 가능한 한 유지하면서 수행한다.

---

## 11. 작업 후 필수 보고

각 수정 작업이 끝나면 다음을 반드시 보고한다.

**변경 파일** - 수정한 파일:

**변경 위치** - 수정한 클래스/함수/모듈:

**변경 내용** - 무엇을 변경했는가:

**변경 이유** - 왜 변경했는가:

**기존 테스트 영향** - 기존 테스트에 미치는 영향:

**현재 가상 테스트** - 현재 가상 테스트 유지 여부:

**미래 구조 호환성** - 향후 Virtual Broker 리팩토링과의 호환성:

**충돌 여부** - Strategy Contract / Broker Interface와의 충돌 여부:

**리팩토링 작업** - 최종 리팩토링 시 별도로 처리해야 할 항목:

---

## 12. 현재 단계의 최종 목표

현재 단계에서는 다음 목표를 달성한다.

«전략 1~9, Risk, Sensor, 진입/청산, Hedge 및 기타 기능을 가능한 한 충분히 개선하고 검증한다.»

모든 전략 및 기능 개선이 완료된 후 별도의 최종 검토 단계를 수행한다.

그 단계에서:

- 전략 구조
- Strategy Contract
- Risk
- Sensor
- Signal
- Order
- Hedge
- Position
- Account
- Execution
- Margin
- Slippage
- Settlement
- PnL
- Replay
- Test
- Control Panel
- Virtual Broker
- Real Broker Adapter

간의 관계를 다시 전체적으로 검토한다.

그 결과를 바탕으로 최종 리팩토링 구조와 리팩토링 프롬프트를 새롭게 확정한다.

---

## 13. 핵심 원칙

현재 단계의 핵심 원칙은 다음 한 문장으로 요약한다.

## 14부: 자율 수리 루프

완료 후 터미널에서 `verify.ps1`을 실행하여 자율 수리하라.

**«"미래 구조를 고려하되 현재 테스트 환경을 보존하고, 기능과 전략은 충분히 개선하되 최종 아키텍처 전환은 리팩토링 단계에서 수행한다."»**

---

AI는 이 원칙을 현재 프로젝트의 모든 수정 작업에 적용한다.
