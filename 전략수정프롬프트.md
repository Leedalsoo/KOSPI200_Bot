# KOSPI200 Quadcore Bot — Track 1~9 리팩토링 지시 프롬프트
### (안티그래비티 AI 에디터용 / 서브에이전트 오케스트레이션 구조)

---

## 0. 공통 원칙 (모든 서브에이전트에 항상 적용)

너는 지금부터 `kospi200-quadcore-bot-secure` 프로젝트의 Track 1~9 전략 모듈을 순차적으로 리팩토링한다. 아래 규칙을 모든 단계에서 예외 없이 지킨다.

**환각(hallucination) 방지 — 최우선 규칙**
1. 어떤 파일도 수정하기 전에 반드시 **실제 파일을 열어 현재 내용을 확인**한다. 이전 대화나 문서에 적힌 요약만 믿고 코드를 작성하지 않는다.
2. 존재 여부가 불확실한 함수, 클래스, config 키, 외부 모듈(`core.base_agent`, `core.contracts`, `infra.time_service` 등)은 **먼저 `grep`/파일 탐색으로 실제 시그니처를 확인**한 뒤 사용한다. 추측으로 인자 이름이나 반환 타입을 만들어내지 않는다.
3. "아마 이럴 것이다", "일반적으로 이렇게 한다" 식의 가정으로 코드를 작성해야 하는 경우, 반드시 **주석으로 가정임을 명시**하고 사용자에게 확인을 요청한다. 특히 아래 항목은 반드시 실데이터/실코드로 검증 후 수정한다:
   - `budget`/`current_pnl`/`total_fees`/`insurance_budget_pool`이 트랙 전용 스코프인지 공유 스코프인지
   - `active_vol`의 정의(배수 vs 실측 변동성)
   - `tick.last_price`가 지수/선물가인지 옵션 프리미엄인지
4. 수정 사항은 항상 **라인 단위 diff**로 제시하고, "무엇이 왜 바뀌는지"를 기존 동작과 비교해 설명한다. 침묵 속에 로직을 바꾸지 않는다.
5. 하나의 트랙 수정이 다른 트랙(특히 Track 1→9, Track 6/7/8 예산 풀)에 영향을 준다면, **그 영향 범위를 먼저 보고하고 승인을 받은 뒤** 진행한다.
6. 각 수정 후 **회귀 테스트를 작성하고 실행**해 통과를 확인한다. 테스트가 없던 모듈은 최소 단위 테스트를 새로 만든다.
7. 확신이 서지 않으면 코드를 임의로 "그럴듯하게" 채우지 말고 **명시적으로 질문한다.**

---

## 1. 서브에이전트 구성 제안

작업량이 크므로 아래와 같이 서브에이전트/스킬을 나눠 오케스트레이션한다 (에디터가 지원하는 경우 병렬화, 미지원 시 순차 수행):

| 서브에이전트 | 역할 |
|---|---|
| **Orchestrator** | 전체 우선순위 관리, Phase 간 의존성 체크, 최종 통합 검증 |
| **Common-Base Implementer** | Phase 0의 공통 유틸/베이스 클래스 구현 |
| **Track Auditor (per track)** | 코드 수정 전, 이전에 정리된 버그 목록이 실제 코드에도 유효한지 재검증 (환각 방지의 핵심) |
| **Track Implementer (per track)** | Auditor가 확인한 버그만 수정, 새로운 버그 추정 금지 |
| **Test Writer** | 각 트랙 수정 후 회귀 테스트 작성 (day-boundary 리셋, 예산 원자성, PnL 계산 등 중심) |
| **Integration Checker** | Track 1↔Track 9 연동, Track 6/7/8 예산 풀 공유 여부 및 원자성 검증 |

---

## 2. Phase 0 — 공통 베이스 (반드시 선행)

9개 트랙에서 반복적으로 발견된 아래 문제들은 개별 트랙에서 각각 고치지 말고, **공통 베이스/유틸로 먼저 만든 뒤 각 트랙이 이를 상속·재사용**하도록 한다.

1. **일자/세션 경계 리셋 믹스인**: `last_trading_date` 비교 → 날짜가 바뀌면 `is_market_opened`, `is_active` 등 상태 플래그를 자동 리셋. Track 1(`is_market_opened` 영구 미리셋), Track 2(`_daily_entry_count` 미리셋), Track 6/7(`bought_date` 자기치유 부재)에 공통 적용.
2. **실시세 기반 PnL/비용 계산 유틸**: 고정 상수(Track1의 2.5pt, Track6의 1.0pt, Track7의 1.4pt, Track8의 1.20/1.50pt)를 실제 체결가/호가 기반 계산으로 교체하는 공통 인터페이스.
3. **예산 풀 원자적 체크-앤-차감 매니저**: Track 6/7/8/9가 공유하는 `insurance_budget_pool`에 대해, 동시 요청 시 이중차감(이중계상)이 발생하지 않도록 락 또는 원자적 트랜잭션으로 처리.
4. **시간 비교 유틸**: `"15:15:00"` 같은 문자열 비교 대신 `datetime.time` 파싱 기반 비교로 통일 (Track 5/6/7 공통 적용).
5. **틱 카운트 기반 타임아웃 → wall-clock 기반으로 전환**하는 공통 타이머 유틸 (Track 3 `cooldown_ticks`, Track 5 `open_time_tick`).

→ 이 Phase가 끝나기 전까지 Track 1~9 개별 로직 수정에 들어가지 않는다.

---

## 3. Phase 1 — Track 1 (Tail Defense) 수정

`Track Auditor`가 아래 항목을 실제 코드에서 재확인 후 `Track Implementer`가 수정:

1. **[최우선]** `is_market_opened`가 날짜 변경 시 리셋되지 않는 버그 → Phase 0의 일자 리셋 믹스인 적용, EOD 청산 로직 추가
2. `base_price(current_atm)`와 스트라이크 계산 기준(`current_underlying`) 불일치 해소
3. `execute_fence_rotation`의 고정 `2.5 * 50000` 이익 → 실체결가 기반으로 교체
4. rotation 쿨다운/히스테리시스 추가 (휩소 시 연쇄 rotation 방지)
5. `check_hedge_exit_conditions`(flatten 포함)를 헷지 활성 여부와 무관하게 매틱 독립 체크하도록 변경 (갭 대응)
6. `trend_signal` 기본값 `True` → 안전한 기본값(`False`) 검토
7. `FENCE_BUILD`/`FENCE_CLEAR` 시그널에 qty 필드 추가

## 4. Phase 2 — Track 2 (Asymmetric Trap) 수정

1. `build_asymmetric_trap`(4-leg 구조)과 `on_tick`(단일 BUY)의 구조적 불일치 해소 — 둘 중 실제 사용할 진입 방식 확정 후 통합
2. `evaluate_trap_status` 스텁 구현 (포지션 모니터링/손절 로직)
3. `_generate_reversal_short_orders` 호출부 연결
4. `_last_loss_time` 실제 세팅 로직 추가 (쿨다운 활성화)
5. `_daily_entry_count` 일자 리셋 추가 (Phase 0 믹스인 사용)
6. 베이시스 필터("역전" 명명 vs "콘탱고 과열" 구현) 의도 확인 후 수정
7. 진입가 quantize 로직을 매도 주문과 동일하게 적용
8. `capital_allocation_rate` 기반 실제 사이징 로직 구현

## 5. Phase 3 — Track 3 (Statistical Arbitrage) 수정

1. **[최우선]** `total_fees`/`current_pnl`의 스코프 확인 — Track 3 전용 격리 필드로 분리
2. 손절 조건 추가 (최대 Z-score 이탈폭 또는 최대 보유시간)
3. 롤오버 구간 스프레드 시계열 리셋/조정 로직 추가
4. 데이터 무효(`spread_history` 부족/`std==0`) 시 `z_score=0.0` 폴백이 조기청산을 유발하지 않도록 HOLD 처리로 변경
5. 사이징 로직 명시 (5% 배분 → 실제 qty)

## 6. Phase 4 — Track 4 (Gamma Scalping) 수정

1. **[최우선]** `_verify_theta_decay_offset`이 델타 헷지 자체를 막는 구조 → 헷지 로직과 분리 (헷지는 항상 수행, 세타 조건은 신규 진입 게이트로만 사용)
2. ATR 정규화 스케일 재검증 (`tick.last_price` 의미 확인 후 K계수/클램핑 범위 재캘리브레이션)
3. High=Low=Close 동일값 문제 → 실제 OHLC 봉 데이터 기반 ATR로 교체
4. `shared_context`의 `current_equity`/`accumulated_profit`/`decay_cost` 네임스페이스 격리 확인
5. 비활성화/deactivation 시 잔여 헷지 포지션 정리 로직 추가
6. `evaluate_scalping_rebalance` dead code 정리

## 7. Phase 5 — Track 5 (Gap Protocol) 수정

1. 문서상 옵션 펜스 압축/원복, 동적 자본배분(+0.1%) 로직 구현 여부 확인 후 반영
2. `active_vol` 파라미터 의미 확인 및 `daily_vol_pct` 산식 검증
3. 손절폭을 `gap_value`에 비례하도록 재설계
4. 타임아웃을 wall-clock 기반으로 전환 (Phase 0 유틸 사용)
5. 부분청산(유동성 공급) 시 qty/잔량 추적 상태 추가
6. 동일 틱 내 조건 우선순위 재정렬 (손절/목표 도달이 유동성공급보다 먼저 체크되도록)

## 8. Phase 6 — Track 6 (Daily Tail Insurance) 수정

1. `bought_date` 자기치유 리셋 추가 (Phase 0 믹스인, 컷오프 누락 대비)
2. `estimated_cost` 고정 상수 → 실시세 기반으로 교체
3. 15:15 비교를 datetime 객체 기반으로 전환
4. 예산 풀 차감 책임 소재를 이 클래스와 예산 관리 모듈 사이에서 명확히 문서화 (이중계상 방지)

## 9. Phase 7 — Track 7 (Weekly Insurance) 수정

1. **[최우선]** `evaluate_take_profit`의 "내재가치만" 계산 로직 → 실제 옵션 평가금액(호가/이론가) 기반으로 교체
2. `bought_date`/`is_week_end` 기반 자기치유 리셋 추가
3. `is_week_end` 산출 로직(만기 캘린더) 별도 검증
4. `estimated_cost` 실시세 기반 교체, `spent` 기본값(350000) 매직넘버 제거
5. **Track 6과의 예산 풀 공유 여부 및 원자적 차감 확인** (Phase 0의 예산 매니저 적용 대상)

## 10. Phase 8 — Track 8 (Monthly Wide Strangle) 수정

1. **[최우선]** DTE 임계치 불일치 해소: 문서(진입≥20/청산≤3) vs 코드(진입≥15/청산≤4) vs 로그(D-4) vs reason 필드(D-3) — 4곳의 표기를 하나로 통일
2. 사이징 공식(`budget/500,000`)을 실제 비용모델(1.20/1.50pt + skew_ratio)에 맞춰 역산 재설계 (예산 초과로 인한 조용한 STANDBY 방지)
3. 저예산 구간 스큐 절사(`int()` truncation) 문제 수정
4. Track 4(감마 스캘핑)로의 명시적 가치 양도 신호 구현 여부 확인

## 11. Phase 9 — Track 9 (Overnight Insurance) 수정

1. **[최우선]** 클래스 내부에 15:15 시간 게이팅 자체 검증 로직 추가 (호출부 실수에 대한 이중 안전장치)
2. 스트라이크별 포지션 상태 추적 도입 — REDUCE 시 정확히 어떤 계약을 줄일지 특정 가능하도록
3. 예산 한도 검사 추가 (Track 1 매도 수량 급증 시 무제한 지출 방지)
4. `max(1, int(active_sell_qty*0.5))` 반올림 규칙 재검토 (저수량 구간 커버리지 왜곡)
5. **Track 1의 Phase 1 수정이 완료된 뒤에 진행** (Track 9는 Track 1의 `active_sell_qty`를 그대로 신뢰하는 구조이므로 순서 종속)

---

## 12. Phase 10 — 통합 검증 (Integration Checker)

1. Track 6/7/8/9가 공유하는 예산 풀에 대해 **동시 트리거 시나리오**(예: 월요일 = 신규 하루 + 신규 주 + 월물 초입 겹침) 테스트 작성 및 실행
2. Track 1 → Track 9 연동 시나리오 테스트 (Track 1 가두리 수량 변화 → Track 9 보험 수량 동기화)
3. 모든 트랙의 "일자/세션 경계" 리셋이 실제로 자정/개장 시점에 정상 동작하는지 멀티데이 시뮬레이션으로 검증
4. 각 트랙에서 제거/변경된 고정 상수(magic number)가 실시세 기반 계산으로 정상 대체되었는지 회귀 테스트로 확인

---

## 13. 완료 조건 체크리스트

- [ ] Phase 0 공통 베이스 구현 및 단위 테스트 통과
- [ ] Track 1~9 각각 위 지시 항목 전부 반영, diff 리뷰 완료
- [ ] 각 트랙 회귀 테스트 신규 작성 및 통과
- [ ] Track 6/7/8/9 예산 풀 원자성 통합 테스트 통과
- [ ] Track 1→9 연동 테스트 통과
- [ ] 멀티데이(최소 3거래일 이상) 시뮬레이션에서 모든 날짜 경계 리셋 정상 동작 확인
- [ ] 모든 "고정 상수 기반 손익/비용 추정"이 실시세 기반으로 교체됨을 코드 리뷰로 확인